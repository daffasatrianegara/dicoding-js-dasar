const arrUser = new Array()
const arrMenu = new Array(5)

const arrUsername = new Array('daffa', 'satria', 'negara', 'prabowo', 'gemoy')
const username = Array.from(arrUsername)

console.log(username);
console.log(Array.from(arrUsername[0]));